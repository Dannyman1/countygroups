# CountyGroups Backend

This folder contains an Express + Mongoose backend scaffold for the CountyGroups project.

Quick start:

1. Copy `.env.example` to `.env` and fill in values.
2. Install dependencies: `npm install`.
3. Run in development: `npm run dev`.

Structure: configs, models, routes, controllers, middlewares, utils, validations.
